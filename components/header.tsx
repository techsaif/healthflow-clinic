"use client";
import Link from 'next/link';import {useState} from 'react';import {clinic} from '@/lib/config';import {Button,Icon} from './ui';
export default function Header(){const[open,setOpen]=useState(false);const nav=[['About','/about'],['Services','/services'],['Doctors','/doctors'],['Patient tools','/track'],['Resources','/blog']];return <header className="sticky top-0 z-40 border-b border-line/70 bg-white/90 backdrop-blur-xl"><div className="container-shell flex h-[76px] items-center justify-between gap-5"><Link href="/" className="flex items-center gap-3" onClick={()=>setOpen(false)}><span className="grid h-10 w-10 place-items-center rounded-2xl bg-navy text-white"><Icon name="cross" size={22}/></span><span><b className="block text-[15px] tracking-tight">Health<span className="text-teal">Flow</span></b><small className="block text-[9px] font-bold uppercase tracking-[.22em] text-slate-400">Clinic</small></span></Link><nav className="hidden items-center gap-7 lg:flex">{nav.map(([label,href])=><Link key={href} href={href} className="text-sm font-semibold text-slate-600 hover:text-navy">{label}</Link>)}</nav><div className="hidden items-center gap-5 lg:flex"><a href={`tel:${clinic.phone}`} className="flex items-center gap-2 text-sm font-bold text-navy"><Icon name="phone" size={16}/>{clinic.phone}</a><Button href="/appointment" icon="calendar">Book appointment</Button></div><button className="grid h-11 w-11 place-items-center rounded-full border border-line lg:hidden" onClick={()=>setOpen(!open)} aria-label={open?'Close menu':'Open menu'}><Icon name={open?'close':'menu'}/></button></div>{open&&<div className="border-t border-line bg-white px-5 py-5 lg:hidden"><nav className="container-shell grid gap-1">{nav.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)} className="rounded-xl px-3 py-3 font-semibold hover:bg-mist">{label}</Link>)}<Link href="/appointment" onClick={()=>setOpen(false)} className="mt-3 inline-flex min-h-12 items-center justify-center rounded-full bg-navy px-5 font-bold text-white">Book appointment →</Link></nav></div>}</header>}
