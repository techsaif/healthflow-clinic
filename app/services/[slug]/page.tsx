import {notFound} from 'next/navigation';import {services} from '@/lib/config';import {StandardPage} from '@/components/sections';import {Button} from '@/components/ui';export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)return notFound();return <><StandardPage eyebrow="Our services" title={service.name} intro="Personalized support, clear communication and a practical plan for what comes next."><section className="container-shell grid gap-10 py-16 md:grid-cols-[1.15fr_.85fr] md:py-24"><div><p className="eyebrow text-teal">What to expect</p><h2 className="mt-3 text-3xl font-extrabold">A visit designed around you.</h2><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">Our {service.name.toLowerCase()} team takes time to understand your concerns, explain options in plain language and coordinate the next step if you need it.</p><div className="mt-8 grid gap-3">{['Easy online booking','Clear care plan','Queue updates while you wait'].map(x=><div key={x} className="flex items-center gap-3 rounded-2xl bg-mist p-4 text-sm font-bold text-navy"><span className="grid h-7 w-7 place-items-center rounded-full bg-teal text-white">✓</span>{x}</div>)}</div></div><div className="rounded-4xl bg-navy p-8 text-white shadow-soft"><p className="eyebrow text-teal">Ready when you are</p><h2 className="mt-4 text-3xl font-extrabold">Make the next step simple.</h2><p className="mt-4 leading-7 text-slate-300">Choose a time that works, then follow your place in line from anywhere.</p><div className="mt-7"><Button href="/appointment" variant="secondary" icon="calendar">Book this service</Button></div></div></section></StandardPage></>}
