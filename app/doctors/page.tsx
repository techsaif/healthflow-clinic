import Image from 'next/image';import Link from 'next/link';import {doctors} from '@/lib/config';import {StandardPage} from '@/components/sections';export default function Page(){return <><StandardPage eyebrow="Our clinicians" title="Meet the people behind the care." intro="Experienced clinicians, a warm front desk and a team that listens before they act."><section className="container-shell grid gap-5 py-16 sm:grid-cols-2 lg:grid-cols-4 md:py-24">{doctors.map(d=><Link key={d.id} href={`/doctors/${d.id}`} className="group overflow-hidden rounded-3xl border border-line bg-white shadow-card"><div className="relative aspect-[.95] overflow-hidden bg-mist"><Image src={d.image} alt={d.name} fill className="object-cover object-top transition duration-500 group-hover:scale-105" sizes="(max-width:768px) 90vw, 24vw"/></div><div className="p-5"><p className="eyebrow text-teal">{d.role}</p><h2 className="mt-2 text-xl font-extrabold">{d.name}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{d.bio}</p><p className="mt-5 text-sm font-bold text-blue">View profile →</p></div></Link>)}</section></StandardPage></>}
