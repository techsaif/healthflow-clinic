import Image from 'next/image';import {notFound} from 'next/navigation';import {doctors} from '@/lib/config';import {Button,PageHero} from '@/components/ui';export function generateStaticParams(){return doctors.map(d=>({id:d.id}))}export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const doctor=doctors.find(d=>d.id===id);if(!doctor)return notFound();return <><PageHero eyebrow={doctor.role} title={doctor.name} intro={doctor.bio}/><section className="container-shell grid gap-10 py-16 md:grid-cols-[.75fr_1.25fr] md:py-24"><div className="relative aspect-[.85] overflow-hidden rounded-4xl bg-mist"><Image src={doctor.image} alt={doctor.name} fill className="object-cover object-top" sizes="(max-width:768px) 90vw, 35vw"/></div><div><p className="eyebrow text-teal">Profile</p><h2 className="mt-3 text-3xl font-extrabold">Care grounded in experience.</h2><p className="mt-5 max-w-xl leading-8 text-slate-600">{doctor.bio} Every visit is designed to leave you with a clear understanding of your options and a confident next step.</p><div className="mt-8 grid gap-5 border-y border-line py-6 sm:grid-cols-3"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Education</p><p className="mt-2 text-sm font-bold text-navy">{doctor.education}</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Experience</p><p className="mt-2 text-sm font-bold text-navy">{doctor.experience}</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Languages</p><p className="mt-2 text-sm font-bold text-navy">{doctor.languages.join(', ')}</p></div></div><div className="mt-8"><Button href="/appointment" icon="calendar">Book with {doctor.name.split(' ').slice(-1)}</Button></div></div></section></>}
