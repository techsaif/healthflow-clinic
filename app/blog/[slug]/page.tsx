import {notFound} from 'next/navigation';import {StandardPage} from '@/components/sections';const posts=[['how-to-prepare-for-a-primary-care-visit','How to prepare for a primary care visit','Health tips'],['the-quiet-power-of-preventive-care','The quiet power of preventive care','Preventive care'],['why-we-built-live-queue-tracking','Why we built live queue tracking','Clinic updates']];export function generateStaticParams(){return posts.map(([slug])=>({slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const post=posts.find(p=>p[0]===slug);if(!post)return notFound();return <><StandardPage eyebrow={post[2]} title={post[1]} intro="A demo article template for HealthFlow Clinic. Replace this content with clinician-reviewed education before launch."><article className="container-shell max-w-3xl py-16 md:py-24"><p className="text-lg leading-8 text-slate-600">Good healthcare starts before the appointment. It starts with clear information, a trusted team and a plan that fits your life. Use this space for medically reviewed guidance, practical preparation checklists and clinic news.</p><h2 className="mt-12 text-3xl font-extrabold">A simple way to prepare</h2><p className="mt-5 leading-8 text-slate-600">Write down your questions, bring an updated medication list and share what has changed since your last visit. Your care team can make better decisions when they have the full picture.</p><div className="mt-10 rounded-3xl bg-mist p-7"><p className="eyebrow text-teal">HealthFlow note</p><p className="mt-3 font-bold text-navy">This educational content is demo content and is not medical advice.</p></div></article></StandardPage></>}
